query VaultAssetsByName($search: String!) {
  vault {
    assets(search: $search, first: 50) {
      nodes {
        name
        type
        description
        createdAt
        updatedAt
        versions {
          version
          createdAt
          filesCount
        }
      }
    }
  }
}
