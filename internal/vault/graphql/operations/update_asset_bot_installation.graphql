mutation UpdateAssetBotInstallation($input: UpdateAssetInput!) {
  updateAsset(input: $input) {
    asset {
      __typename
      id
      slug
      name
      type
      latestVersion
    }
    errors {
      field
      messages
    }
  }
}
