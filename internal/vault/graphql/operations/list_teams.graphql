query ListTeams($first: Int!) {
  organization {
    teams(first: $first) {
      nodes {
        id
        name
        adminMemberIds
        members(first: $first) {
          nodes {
            id
            email
          }
        }
        skillsRepositories {
          repositoryId
        }
      }
    }
  }
}
